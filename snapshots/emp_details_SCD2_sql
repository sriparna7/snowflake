{% snapshot emp_details_SCD2 %} 
     
{{   
    config( 
        target_schema='dbt_snapshots", 
        unique_key='emp_id', 
        strategy='check', 
        check_cols=['emp_id','dept']  
) 
}} 

SELECT 
    emp.emp_id,dept.emp_dept.dept
FROM {{ source('employee_scd') }} emp,
SNOW_DBT_ETL.ETL_CONNECT.EMP_DEPT
where emp.emp_id =EMP_DEPT.emp_id 

{% endsnapshot %}